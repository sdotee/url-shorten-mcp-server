# Server configuration
PORT=3000
HOST=0.0.0.0

# Use 127.0.0.1 to bind only to localhost (more secure for development)
# HOST=127.0.0.1

# Use 0.0.0.0 to bind to all network interfaces (default)
# HOST=0.0.0.0

# Use a specific IP address to bind to a particular network interface
# HOST=192.168.1.100

# URL Shortener API Configuration
URL_SHORTENER_API_KEY=your_api_key_here
URL_SHORTENER_API_BASE=https://s.ee
URL_SHORTENER_DEFAULT_DOMAIN=s.ee
URL_SHORTENER_TIMEOUT=10000
